export const patchNotes = `### Bug Fixes

* **alarms:** fixed alarms with same weather transition crashing the system.
* **currency-spending:** now using server prices only for price checking.
* **leveling-equipment:** cannot wear the same unique ring twice anymore.`;
