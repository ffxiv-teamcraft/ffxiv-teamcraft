export const patchNotes = `### Bug Fixes

* **auth:** fixed a bug preventing anonymous users from creating lists or using the simulator.
* **desktop:** load opcodes from github if not loading from CN region.`;
