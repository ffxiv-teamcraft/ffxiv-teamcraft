export const patchNotes = `### Bug Fixes

* **desktop:** app should now properly open on last visited link.
* **lists:** fixed a bug preventing lists from updating properly in database.
* **lists:** fixed navigation breakdown not marking final items as done properly.`;
