export interface LazySubmarineRank {
  id:                number;
  capacity:          number;
  expToNext:         number;
  surveillanceBonus: number;
  retrievalBonus:    number;
  speedBonus:        number;
  rangeBonus:        number;
  favorBonus:        number;
}
