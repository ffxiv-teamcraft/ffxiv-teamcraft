export interface CardStats {
  numeric: {
    top: string;
    right: string;
    bottom: string;
    left: string;
  };

  formatted: {
    top: string;
    right: string;
    bottom: string;
    left: string;
  };
}
