export enum RotationTipType {
  // Error means that the rotation can't work properly
  ERROR,
  // Warning means that it's a major issue, and it should be solved but it doesn't break the rotation
  WARNING,
  // Info is for small tips
  INFO
}
