export interface LazySubmarineRank {
  capacity:          number;
  expToNext:         number;
  favorBonus:        number;
  id:                number;
  rangeBonus:        number;
  retrievalBonus:    number;
  speedBonus:        number;
  surveillanceBonus: number;
}
